# Bootstrap-минимум user-service: имя приложения (под ним ищется user-service.yml на config-server),
# импорт централизованного конфига и регистрация в Eureka. Остальные настройки приходят из Config Server.
#
# CONFIG_SERVER_URL задаётся окружением:
#   - локально: http://localhost:8888
#   - в docker: http://config-server:8888 (имя контейнера)
spring.application.name=user-service

spring.config.import=optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}

eureka.client.service-url.defaultZone=${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:8761/eureka}
